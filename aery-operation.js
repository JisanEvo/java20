// write a function to give me the sum of all number in an array

function sumOfNumber(number) {
    let sum = 0;
    for (const number of numbs) {
        console.log(number);
        sum = sum + number
    }

    return sum;
}
const numbs = [1, 25, 36, 48, 69, 75, 122]
const sum = sumOfNumber(numbs);
console.log(`sum of Number:`, sum);